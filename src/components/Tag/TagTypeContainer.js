import React from 'react';
import styled from 'styled-components';

const TagTypeContainer = styled.li`
  padding-top: 10px;
  padding-bottom: 10px;
  border-bottom: 0.05px solid black;
  text-transform: uppercase;
`;

export default TagTypeContainer;
